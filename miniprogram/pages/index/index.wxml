<!-- <button class="weui-btn" type="primary" bindtap="showAction">弹出ActionSheet</button> -->
<!-- <mp-actionSheet bindactiontap="btnClick" show="{{showActionsheet}}" actions="{{groups}}" title="这是一个标题，可以为一行或者两行。">
</mp-actionSheet> -->
<view data-cfb-theme="{{theme}}">
				<view class="test-theme-class">主题测试：{{theme}}</view>
				<button class="weui-btn" type="primary" bindtap="showModal">确定</button>
				<mp-dialog title="test" show="{{dialogShow}}" bindbuttontap="tapDialogButton" buttons="{{buttons}}">
								<mp-msg type="success" title="操作成功"></mp-msg>
				</mp-dialog>
</view>